@model PaiAirlines.Models.Booking

@{
    ViewData["Title"] = "Edit";
    List<Flight> flights = (List<Flight>)ViewData["FlightList"];
}

<h2>Edit</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <h4>Booking</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="ID" />
        <input type="hidden" asp-for="Userid" />
        <input type="hidden" asp-for="FlightID" />

        @*<div class="form-group">
            <label class="col-md-2 control-label">Flight Number</label>
            <div class="col-md-10">
                <select name="Flight">
                    @foreach (Flight flt in flights)
                    {
                        <option value="@flt.ID">@flt.FlightNumber</option>
                    }
                </select>
            </div>
        </div>*@
        <div class="form-group">
            <label asp-for="SeatsAmount" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="SeatsAmount" class="form-control" />
                <span asp-validation-for="SeatsAmount" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <label asp-for="TotalPrice" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="TotalPrice" class="form-control" />
                <span asp-validation-for="TotalPrice" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

